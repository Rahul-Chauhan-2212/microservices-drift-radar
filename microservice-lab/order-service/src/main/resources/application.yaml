spring:
  application:
    name: order-service
server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    prometheus:
      enabled: true

management.metrics.tags.application: ${spring.application.name}